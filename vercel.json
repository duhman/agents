{
  "buildCommand": "pnpm install && pnpm build",
  "crons": [
    {
      "path": "/api/cron/process-slack-retry",
      "schedule": "*/5 * * * *"
    }
  ],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
